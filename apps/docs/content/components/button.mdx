import { ComponentPreview } from "@/components/ComponentPreview";
import { Tabs, Steps } from "nextra/components";
import { Button } from "@nuvra-ui/react";
import { FaArrowRight } from "react-icons/fa";

# Button

An interactive component to allow users to perform actions.

<Tabs items={['Preview', 'Usage']}>
  <Tabs.Tab>
    <ComponentPreview>
      <Button>Click me!</Button>
    </ComponentPreview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```tsx filename="BadgeDemo.tsx"
      import { Button } from "@nuvra-ui/react";
      
      export function ButtonDemo() {
        return (
          <Button>Click me!</Button>
        )
      }
    ```
  </Tabs.Tab>
</Tabs>

## Setup

<Steps>

### Import the component

```tsx
import { Button } from "@nuvra-ui/react";
```

### Customize the component Style

```tsx filename="BadgeDemo.tsx"
import { Button } from "@nuvra-ui/react";

export function ButtonDemo() {
  return (
    <div className="flex gap-4">
      <Button className="bg-white hover:bg-gray-200 border border-gray-400 text-gray-600">
        Nuvra-UI
      </Button>
      <Button className="gap-2">
        Icon
        <FaArrowRight />
      </Button>
    </div>
  );
}
```

### Preview the component

<ComponentPreview>
  <div className="flex gap-4">
    <Button className="bg-white hover:bg-gray-200 border border-gray-400 text-gray-600">
      Nuvra-UI
    </Button>
    <Button className="gap-2">
      Icon
      <FaArrowRight />
    </Button>
  </div>
</ComponentPreview>

</Steps>
